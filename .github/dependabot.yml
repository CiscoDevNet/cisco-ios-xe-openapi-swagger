# Dependabot configuration
# This project has no npm/pip runtime dependencies.
# Swagger UI is loaded from CDN, not from node_modules.
# Only monitor GitHub Actions for version updates.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
